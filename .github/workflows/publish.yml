name: Release to NPM
on:
  push:
    branches: [main]
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24.15.0"
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        working-directory: stellar-protocol-sdk
        run: npm ci

      - name: Build
        working-directory: stellar-protocol-sdk
        run: npm run build

      - name: Publish to NPM
        working-directory: stellar-protocol-sdk
        run: |
          VERSION=$(node -p "require('./package.json').version")
          if [[ "$VERSION" == *"-"* ]]; then
            echo "Publishing prerelease version $VERSION with tag 'rc'"
            npm publish --access public --tag rc
          else
            echo "Publishing stable version $VERSION"
            npm publish --access public
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_CODE }}
